---
import Layout from '../layouts/Layout.astro';

// Page Metadata
const pageTitle = "Financial Highlights | Shakarganj Limited";
const metaDescription = "Multi-year financial performance highlights, operational metrics, profitability, balance sheet summary, and key financial ratios for Shakarganj Limited.";

// Cloudflare R2 hosted PDF & local fallback
const pdfUrl = "https://docs.sml.com.pk/docs/SML-Financial-Highlights.pdf";
const localPdfUrl = "/docs/SML-Financial-Highlights.pdf";
---

<Layout title={pageTitle} description={metaDescription}>
  <!-- Hero Section -->
  <section class="highlights_hero">
    <div class="container">
      <div class="breadcrumbs">
        <a href="/" class="home_link">Home</a>
        <span class="sep"><i class="fa fa-angle-right"></i></span>
        <a href="#" class="sub_link">Investor Relations</a>
        <span class="sep"><i class="fa fa-angle-right"></i></span>
        <span class="current">Financial Highlights</span>
      </div>

      <div class="hero_content_flex">
        <div class="hero_text_box">
          <span class="hero_badge">Investor Relations • Performance Metrics</span>
          <h1 class="hero_title">Financial Highlights</h1>
          <p class="hero_subtitle">
            Comprehensive financial performance indicators, multi-year revenue growth, capital expenditure, profitability metrics, and balance sheet summary for Shakarganj Limited.
          </p>
        </div>

        <div class="hero_actions">
          <a href={pdfUrl} target="_blank" rel="noopener noreferrer" class="btn_action download_btn" download="SML-Financial-Highlights.pdf">
            <i class="fa fa-download"></i> Download PDF
          </a>
          <a href={pdfUrl} target="_blank" rel="noopener noreferrer" class="btn_action fullscreen_btn">
            <i class="fa fa-external-link"></i> Open Fullscreen
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- PDF Viewer Section -->
  <section class="pdf_viewer_section">
    <div class="container">
      
      <!-- Viewer Container Card -->
      <div class="pdf_card_wrapper">
        <!-- Embedded Full Page PDF Viewer (Toolbar Hidden for Pure Content View) -->
        <div class="pdf_embed_container">
          <object
            data={`${pdfUrl}#toolbar=0&navpanes=0&scrollbar=1&view=FitH`}
            type="application/pdf"
            class="pdf_object"
          >
            <iframe 
              src={`${pdfUrl}#toolbar=0&navpanes=0&scrollbar=1&view=FitH`}
              title="Shakarganj Limited Financial Highlights PDF"
              class="pdf_iframe"
              loading="lazy"
            >
              <p>Your browser does not support embedded PDF viewing. 
                <a href={pdfUrl} target="_blank" rel="noopener noreferrer">Click here to view or download the PDF document directly.</a>
              </p>
            </iframe>
          </object>
        </div>
      </div>

    </div>
  </section>
</Layout>

<style>
  /* Hero Section */
  .highlights_hero {
    background: linear-gradient(135deg, #0b1d12 0%, #15321f 50%, #0d2215 100%);
    padding: 35px 0 45px 0;
    color: #ffffff;
    border-bottom: 3px solid var(--primary-color);
  }

  .highlights_hero .breadcrumbs {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 13px;
    color: #a0aec0;
    margin-bottom: 20px;
  }

  .highlights_hero .breadcrumbs a {
    color: #cbd5e0;
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .highlights_hero .breadcrumbs a:hover {
    color: var(--primary-color);
  }

  .highlights_hero .breadcrumbs .sep {
    color: #718096;
    font-size: 11px;
  }

  .highlights_hero .breadcrumbs .current {
    color: #ffffff;
    font-weight: 600;
  }

  .hero_content_flex {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    gap: 24px;
    flex-wrap: wrap;
  }

  .hero_text_box {
    max-width: 750px;
  }

  .hero_badge {
    display: inline-block;
    padding: 4px 12px;
    background: rgba(46, 125, 50, 0.25);
    border: 1px solid rgba(76, 175, 80, 0.4);
    color: #81c784;
    font-size: 12px;
    font-weight: 600;
    border-radius: 20px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-bottom: 12px;
  }

  .hero_title {
    font-size: 32px;
    font-weight: 700;
    margin: 0 0 10px 0;
    color: #ffffff;
  }

  .hero_subtitle {
    font-size: 15px;
    color: #cbd5e0;
    margin: 0;
    line-height: 1.6;
  }

  .hero_actions {
    display: flex;
    align-items: center;
    gap: 10px;
    flex-wrap: wrap;
  }

  .btn_action {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 9px 18px;
    border-radius: 6px;
    font-size: 13px;
    font-weight: 600;
    text-decoration: none;
    cursor: pointer;
    transition: all 0.25s ease;
  }

  .download_btn {
    background: var(--primary-color);
    color: #ffffff;
    border: none;
    box-shadow: 0 4px 12px rgba(46, 125, 50, 0.3);
  }

  .download_btn:hover {
    background: var(--primary-hover);
    transform: translateY(-2px);
  }

  .fullscreen_btn {
    background: rgba(255, 255, 255, 0.1);
    color: #ffffff;
    border: 1px solid rgba(255, 255, 255, 0.2);
  }

  .fullscreen_btn:hover {
    background: rgba(255, 255, 255, 0.2);
    color: #ffffff;
  }

  .print_btn {
    background: transparent;
    color: #cbd5e0;
    border: 1px solid #4a5568;
  }

  .print_btn:hover {
    background: #2d3748;
    color: #ffffff;
  }

  /* PDF Viewer Section */
  .pdf_viewer_section {
    padding: 40px 0 60px 0;
    background: #f8fafc;
  }

  .pdf_card_wrapper {
    background: #ffffff;
    border-radius: 12px;
    border: 1px solid #cbd5e0;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
    overflow: hidden;
  }

  .pdf_toolbar_strip {
    background: #1e293b;
    color: #ffffff;
    padding: 12px 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid #334155;
  }

  .toolbar_left {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 14px;
    font-weight: 600;
  }

  .pdf_icon {
    color: #ef4444;
    font-size: 18px;
  }

  .badge_r2_live {
    font-size: 11px;
    font-weight: 600;
    padding: 3px 10px;
    border-radius: 12px;
    background: rgba(59, 130, 246, 0.2);
    color: #60a5fa;
    border: 1px solid rgba(96, 165, 250, 0.3);
  }

  .pdf_embed_container {
    width: 100%;
    height: clamp(600px, 82vh, 1050px);
    background: #525659;
    position: relative;
    overflow: hidden;
  }

  .pdf_object,
  .pdf_iframe {
    width: 100%;
    height: 100%;
    border: none;
    display: block;
    overflow: hidden;
  }

  .pdf_card_footer {
    padding: 14px 20px;
    background: #f1f5f9;
    border-top: 1px solid #e2e8f0;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 12px;
  }

  .footer_note {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 13px;
    color: #475569;
  }

  .footer_note i {
    color: var(--primary-color);
  }

  .footer_note a {
    color: var(--primary-color);
    font-weight: 600;
    text-decoration: none;
  }

  .footer_note a:hover {
    text-decoration: underline;
  }

  .direct_link {
    font-size: 13px;
    font-weight: 600;
    color: #1e293b;
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    gap: 6px;
    transition: color 0.2s ease;
  }

  .direct_link:hover {
    color: var(--primary-color);
  }

  /* Responsive Styling */
  @media (max-width: 768px) {
    .highlights_hero {
      padding: 25px 0 35px 0;
    }

    .hero_title {
      font-size: 24px;
    }

    .hero_content_flex {
      flex-direction: column;
      align-items: stretch;
    }

    .hero_actions {
      width: 100%;
      justify-content: flex-start;
    }

    .pdf_embed_container {
      height: 600px;
    }
  }
</style>
